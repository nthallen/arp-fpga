This archive includes the files listed in the MANIFEST file.

After unpacking the archive, you need to do a few things:

Copy ssp_log.exe and ssp_cmd.exe into /usr/local/bin
Make sure /usr/local/bin (probably c:\cygwin\usr\local\bin) is
  in your system PATH
Add these two programs to the list of firewall exceptions.
Create a directory for Matlab SSP driver files (SSP_MATLAB_DIR)
Copy the files in the Matlab subdirectory into the SSP_MATLAB_DIR
Add the SSP_MATLAB_DIR to your Matlab Path

